{
  "name": "@addnewfeature/feedback-lib-launcher",
  "version": "0.1.0",
  "private": true,
  "description": "Addnewfeature's private launcher package — implements @claudecontrol/feedback-lib's FeedbackBackend via Claude Code + tmux + the automatelinux daemon.",
  "main": "./index.ts",
  "types": "./index.ts",
  "peerDependencies": {
    "@claudecontrol/feedback-lib": "workspace:*",
    "next": ">=14",
    "react": ">=18 <20"
  }
}
