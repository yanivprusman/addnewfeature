import type { MaintenancePrompt } from './issues-page-types';

export const MAINTENANCE_PROMPTS: MaintenancePrompt[] = [
  {
    id: "feedback-context",
    title: "Add feedback context tracking",
    description: "Ensure tab/section navigation sets data-active-tab on the active element so the feedback widget tracks which view the user is on.",
    prompt: "Scan this app for tab or section navigation (tab bars, sidebars, segmented controls). For each one, ensure the currently active element has a `data-active-tab` attribute set to the visible label text. This attribute must move with the active state — only the active element should have it at any given time. The feedback-lib widget reads all `data-active-tab` elements via `document.querySelectorAll('[data-active-tab]')` at issue-report time and joins them with ' > ' (e.g. 'Claude > All'). Do not remove any existing attributes. Commit and push when done.",
  },
  {
    id: "assign-data-ids",
    title: "Assign data-id to all significant UI elements",
    description: "Scan for interactive elements AND significant non-interactive UI sections missing a data-id attribute and add appropriate values.",
    prompt: "Add a `data-id` attribute to EVERY rendered HTML element in this app's source tree that does not already have one. **No exceptions.** A data-id on the component root and on interactive elements is NOT enough — every `<div>`, `<span>`, `<p>`, `<li>`, `<td>`, badge, status pill, and text-bearing span must have one.\n\n**Scope:** All `.tsx` / `.jsx` files under this app's source tree (`components/`, `app/`, `src/`, `pages/`, etc.). Skip `node_modules/`, `.next/`, `dist/`, `build/`, and shadcn/ui primitive files under `components/ui/` (those are auto-reused). The scanner below enforces this scope automatically.\n\n**Which tags need data-id:** lowercase DOM tags — `div`, `span`, `section`, `p`, `h1`–`h6`, `img`, `a`, `button`, `input`, `textarea`, `select`, `option`, `ul`, `ol`, `li`, `table`, `thead`, `tbody`, `tr`, `td`, `th`, `form`, `label`, `pre`, `code`, `nav`, `header`, `footer`, `aside`, `main`, `article`, `figure`, `figcaption`. Do NOT add to React component invocations like `<Button>` — that's the component's own job. If a React component forwards props to a DOM node, tag the DOM node inside the component's implementation.\n\n**Commonly-missed categories — check each one explicitly:**\n- Display spans with interpolated/computed values: `<span>{percentage}%</span>`, `<span>{formatDate(x)}</span>`, `<span>{user.name}</span>`. Each one needs a data-id.\n- Elements inside `<TooltipContent>`, `<DialogContent>`, `<PopoverContent>`, `<DropdownMenuContent>`. Tooltip contents are the #1 place people forget.\n- Early-return branches: a component with `if (loading) return <div/>; if (error) return <div/>; return <div/>;` needs data-id on ALL THREE root divs, not just the main one.\n- Conditional branches: `{a ? <span/> : <span/>}` — both branches.\n- Elements inside `.map()` loops — use a template literal with the item's key: `` data-id={`user-row-${user.id}`} ``.\n- Table cells (`<td>`, `<th>`) showing data.\n- Badges, pills, status dots, small icons with semantic meaning.\n- Text inside `<label>`, `<legend>`, `<p>`, headings.\n- Sub-components defined in the same file (inner named functions returning JSX) — each of their roots need a data-id too.\n- Helper functions that return JSX fragments used from a parent.\n\n**Naming conventions:**\n1. Component root: PascalCase matching the component — `data-id=\"UserProfile\"`. If a component has multiple early-return branches, they can all share that same root name.\n2. Interactive elements (button/input/onClick handler): descriptive kebab-case — `data-id=\"save-settings\"`, `data-id=\"clear-filter\"`.\n3. Display spans: kebab-case describing the data shown — `data-id=\"user-name\"`, `data-id=\"last-login-time\"`, `data-id=\"session-exhausts-at\"`.\n4. Map/loop items: template literal with a stable key — `` data-id={`row-${item.id}`} ``.\n5. Pure layout wrappers still get a data-id — use a generic name like `data-id=\"header-row\"` or `data-id=\"content-wrapper\"`.\n\n**Do not modify elements that already have a `data-id`.**\n\n**Verification — MANDATORY. This is the step people skip and that's why audits fail.**\n\nRun the JSX-aware scanner that ships with feedback-lib:\n\n```bash\npython3 lib/feedback-lib/scan-missing-data-ids.py .\n```\n\n(Adjust the path to feedback-lib if your app lays it out differently — e.g. `src/lib/feedback-lib/scan-missing-data-ids.py`.)\n\nThe scanner:\n- Uses a JSX-aware parser that tracks brace depth and string state, so `=>` in arrow functions inside attributes (`onClick={e => e.stopPropagation()}`) is handled correctly — a naive regex would break on these.\n- Prints every file/line/tag that is missing a data-id.\n- Exits 0 with `TOTAL MISSING: 0` when fully clean, 1 otherwise.\n\n**Iteration loop — follow exactly:**\n1. Run the scanner.\n2. Read the first reported file, add `data-id` to every reported element in it.\n3. Re-run the scanner.\n4. Repeat until it prints `TOTAL MISSING: 0` and exits 0.\n\n**Do not fake the verification.** Do not modify the scanner to pass. Do not commit while any misses remain. The scanner is authoritative — if it reports a miss, that element is missing a data-id, period.\n\n**Finish:** Run `npm run build` (or the equivalent build command) to confirm nothing broke, then commit and push. The commit should only happen after the scanner reports zero AND the build passes.",
  },
  {
    id: "refactor-large-files",
    title: "Refactor large files",
    description: "Scan for large code files and refactor them by splitting into smaller files or extracting shared utilities.",
    prompt: "Scan all source files in this app and identify files that are excessively large (roughly 300+ lines). Cover both:\n- **TypeScript/JavaScript**: `.ts`, `.tsx`, `.js`, `.jsx`\n- **C++**: `.cpp`, `.cc`, `.cxx`, `.h`, `.hpp`, `.hxx`\n\nFor each large file, use your judgment to refactor it in one or more of these ways, using conventions appropriate to the language:\n\n1. **Split into smaller files**: If the file contains multiple distinct components, functions, classes, or logical sections, extract them into separate files and update imports/includes.\n   - TS/JS: extract components, hooks, or function groups into their own modules and update `import` statements.\n   - C++: split unrelated classes or free-function groups into their own translation units; keep one primary class per `.h`/`.cpp` pair where practical, and update `#include` directives.\n2. **Extract shared utilities**: If you find repeated logic across the file (or across multiple files), extract it into a shared utility module.\n   - TS/JS: a shared `utils/` or `lib/` module.\n   - C++: a dedicated utility header/source pair (e.g. `foo_utils.h` / `foo_utils.cpp`) in an appropriate namespace.\n3. **Separate types/interfaces/declarations**:\n   - TS/JS: move large type/interface definitions to a dedicated `types.ts` file.\n   - C++: ensure the header/source split is clean — declarations in `.h`/`.hpp`, definitions in `.cpp`/`.cc`. If a header has grown large with many unrelated type/struct/class declarations, split it into focused headers. Move heavy inline method bodies out of headers into their `.cpp` counterparts when they don't need to be inlined.\n\nPrioritize readability and maintainability. A file is only exempt from splitting if it is truly cohesive — one focused responsibility that would genuinely become harder to follow if separated. A single React component is NOT automatically cohesive: if it contains multiple distinct dialog/modal overlays, multiple independent state groups (e.g. 10+ useState hooks for separate features), or inline sub-features that could be their own component, it should be split. Likewise, a single C++ class is not automatically cohesive: if it mixes unrelated concerns (e.g. I/O, parsing, and business logic) or has many large unrelated method groups, split it. When in doubt, split. Skip test files, generated files, build artifacts, vendored/third-party code, and config files. After refactoring C++ sources, ensure the project still builds (`d buildApp --app <name>` if applicable). Commit and push when done.",
  },
];
